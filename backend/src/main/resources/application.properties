# 애플리케이션 이름
spring.application.name=backend

# 서버 포트
server.port=8080

# MySQL 연결 정보
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/trma?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#spring.datasource.url=jdbc:mysql://127.0.0.1:3306/trma?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=fkadmin
spring.datasource.password=Dlatl00!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.example.backend.trma.dto

# JPA 설정
spring.jpa.hibernate.ddl-auto=validate

# 실행 SQL 출력
spring.jpa.show-sql=true

# SQL 보기 좋게 출력
spring.jpa.properties.hibernate.format_sql=true

# 개발 편의 옵션
spring.jpa.open-in-view=false

# jjwt
jwt.secret=trma-secret-key-2026-this-key-must-be-long-enough-123456789
jwt.expiration=21600000